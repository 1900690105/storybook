"use client";
// Placeholder component to keep structure clean. Do NOT import this yet.
// Move the Choose Format cards and actions from
// `src/app/visitor/home/shoppingpage.jsx` here later.

export default function FormatCards(/* { formats, selectedFormat, setSelectedFormat, onCancel, onContinue } */) {
  return null;
}
